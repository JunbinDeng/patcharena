name: demo-task
repo_path: /absolute/path/to/local/git/repo
prompt: |
  Fix the failing behavior and keep the project runnable.
compile_command: ""
test_command: pytest
agent_timeout: 1800
agents:
  - codex
  - claude
  - opencode
  - copilot
  # - claude-bedrock   # requires AWS credentials + Bedrock model access

# agent_options:       # optional per-agent config
#   claude-bedrock:
#     model: ""        # Bedrock model ID; find with: aws bedrock list-foundation-models --by-provider anthropic
#     region: ""       # AWS region; omit to use AWS_REGION env var
